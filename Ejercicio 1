/////Familiarizarse con la estructura de selección, (If, If-else, SWitch)/////
//insertar ficheros en el header//

#include <stdio.h>
int main()
{
//Declarar variables globales (funciones)//
    int a, b;
    a = 3;
    b = 2;
//Procesos//
if ( a > b )
  {
     printf("\ta (%d) es mayor a b (%d). \n" ,a,b);
  }
  printf("\t\vE1 programa sigue su flujo.\n");
  return 0;
}
